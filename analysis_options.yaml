# Lint warnings are suppressed project-wide to preserve the C#-style code structure.
# The translated Dart code intentionally uses C# naming conventions (PascalCase methods,
# m-prefixed fields, etc.) to maintain close correspondence with the original source.
